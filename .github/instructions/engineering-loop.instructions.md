---
applyTo: "**"
---
# Engineering loop

Plan → failing test → smallest change → full test run → review → session report. Each step leaves an artifact the next step can open. Skipping a step is allowed only when the session report says which step and why.

---
applyTo: "sample_app/**,tests/**"
---
# Sample application

Python 3.11, no dependencies beyond the standard library and `pytest`. Run tests with `python -m pytest -q`. Keep functions small and typed. A change here needs a test in `tests/` in the same pull request.

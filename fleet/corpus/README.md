# Corpus

Facts the fleet should not have to rediscover. One file per topic. Every fact carries its source: a command and its output, a document, or a person's decision with a date. `corpus-librarian` reads here first and writes back in the same session.

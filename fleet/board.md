# Board

Rebuilt by every sweep. One row per open item.

| Item | Owner | State | Next action | Evidence |
|------|-------|-------|-------------|----------|
| KI-1 percent discount applied as flat amount | `implementer` | triaged | write the fix after `test_percent_discount` is confirmed failing | `fleet/corpus/known-issues.md` |

**Next for a human:** review the KI-1 pull request when it opens.
